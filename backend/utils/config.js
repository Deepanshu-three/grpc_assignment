export const PROTO_FILE_PATH = "./proto/question.proto";

export const GRPC_HOST = "localhost:50052";

export const PROTO_FILE_LOAD_OPTIONS = {
    keepCase: true,
    longs: String,
    enums: String,
    defaults: true,
    oneofs: true
  }